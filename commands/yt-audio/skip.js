const { prefix } = require('../../config/command-config')

module.exports = {
    name: `${prefix}play`,
    description: 'Skip track',
    paramsRequired: true,
    execute(msg, args, params) {

        
    }
}
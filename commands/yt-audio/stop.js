const { prefix } = require('../../config/command-config')

module.exports = {
    name: `${prefix}stop`,
    description: 'Stop audio playing',
    paramsRequired: true,
    execute(msg, args, params) {

        
    }
}